/** Displays the placeholder boundary for execution monitoring. */
export function ExecutionPage(): React.JSX.Element {
  return <section><span className="eyebrow">Run</span><h1>Executions</h1><p>Track active and completed PROVA test runs.</p></section>;
}

/** Displays the placeholder boundary for workspace settings. */
export function SettingsPage(): React.JSX.Element {
  return <section><span className="eyebrow">Configure</span><h1>Settings</h1><p>Manage your Studio workspace and CLI connection.</p></section>;
}

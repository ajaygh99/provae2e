# Release Approval — v0.3.5-beta.2

Decision: **APPROVED FOR CONTROLLED BETA**

Date: 2026-08-09

Approved by:

- Ajay — owner authorization provided in the release-preparation target goal.
- Codex — technical approval conditional on the release PR and protected SHIP
  workflow completing successfully.

Release conditions:

- Publish only from `main` through the manual SHIP workflow.
- The release PR must pass all required GitHub Actions and CodeQL checks.
- The package version, lockfile version, Studio version, notes, approval, npm
  tag, Git tag, and GitHub prerelease must agree on `0.3.5-beta.2`.
- Package dry-run and clean-install smoke validation must pass before dispatch.
- Preserve generated evidence and all integration credentials outside Git.
- Advertise this as a controlled beta; do not represent it as GA.

Evidence:

- PR #324 merged to `main` with all ten checks passing.
- Phase 4 combined automation completed with 8 passed and 0 failed.
- Scenario 1 passed in Chromium, Firefox, Edge, and Playwright WebKit.
- Studio tests and build, CLI build, secret-safety, LENS, and CodeQL passed.

# PROVA v0.3.4-beta.1 — Slice 1 Evidence

**Status:** PASS — ready for owner approval
**Evidence commit:** `129a26d25235ed94312fdfac1e2a3dd8f2dcd8b8`
**Cross-platform CI:** [GitHub Actions run 30285089687](https://github.com/ajaygh99/provae2e/actions/runs/30285089687)
**Evidence generated:** 2026-07-27

## Exit-gate result

| Gate | Required | Observed | Result |
|---|---:|---:|---|
| Clean install and packed CLI smoke | Windows, Ubuntu, macOS | Passed on all three operating systems | PASS |
| First installed-CLI run | Under 10 minutes | Slowest complete validation step was under 1 minute | PASS |
| Smoke flake rate | Below 2% over 100 runs | 0/100 failed; 0.00% | PASS |
| Test coverage | At least 80% | Statements 94.45%; branches 88.65%; functions 97.26%; lines 94.45% | PASS |
| Production dependency audit | 0 critical/high | 0 critical, 0 high, 0 total vulnerabilities | PASS |
| v0.3.x CLI compatibility | No regression of previously shipped CLI contract | Existing v0.3.1 and v0.3.2 run options retained in v0.3.3 | PASS |

## 1. Genuine 100-run smoke campaign

The corrected evidence runner executed `npm run test:smoke` 100 separate times on Windows. It rejects process-launch errors, non-zero exit codes, and zero-test executions.

- Execution window: `2026-07-27T20:17:32.710Z`–`2026-07-27T20:44:30.050Z`
- Total elapsed time: 26.96 minutes
- Passing runs: 100
- Failing runs: 0
- Flake rate: 0.00%
- Tests per run: 3 passed, 0 failed, 0 skipped, 3 total
- Total test executions: 300
- Average process duration: 16.172 seconds
- Raw evidence: `releases/smoke-logs/run-001.log` through `run-100.log`
- Machine-readable manifest: `releases/smoke-runs-manifest-integrity.json`

Each run exercises browser testing, a live local HTTP API request, and Playwright mobile-browser emulation using the iPhone 14 device descriptor.

Manifest reconciliation checks passed:

- exactly 100 run entries;
- 100 unique run numbers and log paths;
- every exit code is zero;
- every run reports exactly 3 executed tests;
- all referenced logs exist and contain matching Jest summaries;
- summed per-run duration equals the manifest aggregate.

## 2. Cross-platform clean-install validation

GitHub Actions run `30285089687` passed the required matrix on Ubuntu, macOS, and Windows. Each job performed a clean dependency installation, lint, type checking, build, full test suite with coverage, package creation, install into a clean consumer project, Playwright browser provisioning, and packed CLI smoke execution.

Observed full-suite result:

- 116 test suites passed;
- 2,007 tests passed;
- 5 tests skipped;
- 2,012 tests total;
- 0 failures.

The packed CLI consumer smoke passed on all three operating systems. The complete installed-CLI validation step took approximately 19 seconds on Ubuntu, 20 seconds on macOS, and 44 seconds on Windows, all well below the 10-minute gate.

## 3. Coverage

Coverage reported by the successful CI run:

| Metric | Observed | Gate |
|---|---:|---:|
| Statements | 94.45% | ≥80% |
| Branches | 88.65% | ≥80% |
| Functions | 97.26% | ≥80% |
| Lines | 94.45% | ≥80% |

All four coverage measures pass the unconditional 80% threshold.

## 4. Security

`npm audit --omit=dev --json` was executed against the current lockfile:

- critical: 0;
- high: 0;
- moderate: 0;
- low: 0;
- total: 0.

The concise machine-readable result is stored in `releases/security-audit-slice1.json`.

## 5. Backward compatibility

Published npm versions and matching Git tags were inspected:

- `0.3.1-beta.0` / `v0.3.1-beta.0`;
- `0.3.2-beta.1` / `v0.3.2-beta.1`;
- `0.3.3-beta.1` / `v0.3.3-beta.1`.

The current executable retains both historical binary aliases (`qe-tool` and `prova`) and every `run` option shipped in v0.3.1 and v0.3.2. Newer BrowserStack, evidence, and analytics options are additive. The current `--help` and `run --help` commands execute successfully after a clean build.

There is no published `0.3.0` npm version or corresponding repository tag, so the compatibility claim is intentionally limited to the verifiable published v0.3.x chain.

## Integrity note

Earlier hand-authored manifests and reports were rejected and removed. This report does not convert a single CI suite run into a 100-run flake claim; the flake result comes only from the separate 100-process smoke campaign. No evidence script stages, commits, pushes, or schedules itself.

## Approval recommendation

Slice 1 meets every mandatory technical gate. Approve Slice 1 and proceed to Slice 2: Failure Evidence Completeness and Secret Safety.

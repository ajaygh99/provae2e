# PROVA v0.2.0 — Phase 2 Intelligence

This release adds the complete Phase 2 CLI intelligence toolset:

- JIRA acceptance-criteria ingestion, OAuth2 helpers, named environments, test generation, and status sync.
- Faker-backed schema-aware test data with deterministic output.
- Figma ingestion and generated component tests with encrypted local credentials.
- AI-assisted Playwright generation from specifications and multilingual acceptance criteria.
- Multi-environment promotion gates for `dev → qe → staging`.
- k6 execution, SQLite performance history, baselines, and regression detection.
- Strict input validation, exponential retries, and nested REST/GraphQL schema validation.

Quality gates: full automated suite, 80% global coverage minimum, Phase 2 Faker/SQLite E2E, and LENS review.
